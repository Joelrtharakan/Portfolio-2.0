diff --git a/node_modules/handlebars/lib/index.js b/node_modules/handlebars/lib/index.js
index 035b822..74f346b 100644
--- a/node_modules/handlebars/lib/index.js
+++ b/node_modules/handlebars/lib/index.js
@@ -1,5 +1,5 @@
 import base from './handlebars/base';
 
 // Each of these augment the Handlebars object. No need to setup a temporary object.
-import { loadFile, require, __dirname } from './handlebars/fs';
+// import { loadFile, require, __dirname } from './handlebars/fs';
 import { ok, Exception, log, createFrame } from './handlebars/utils';
 import {
   COMPILER_REVISION,
@@ -29,11 +29,11 @@ import runtime from './handlebars/runtime';
 const {
   HandlebarsEnvironment,
   SafeString,
-  Visitor,
+  Visitor
 } = runtime;
 
 function create() {
-  let hb = new base.HandlebarsEnvironment();
+  let hb = new base.HandlebarsEnvironment();  
 
   ok(hb.compile, 'Handlebars not loaded');
 
@@ -62,7 +62,7 @@
   ret.logger = logger;
   ret.log = log;
 
-  if (loadFile) {
-    ret.loadFile = loadFile;
-  }
+  // if (loadFile) {
+  //   ret.loadFile = loadFile;
+  // }
 
   if (noConflict) {
     ret.noConflict = noConflict;
